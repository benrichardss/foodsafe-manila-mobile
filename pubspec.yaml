name: foodsafe_manila
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.0

dependencies:
  fl_chart: ^1.1.1
  flutter:
    sdk: flutter
  flutter_map: ^8.2.2
  flutter_map_location_marker: ^10.2.0
  flutter_screenutil: ^5.9.3
  geocoding: ^4.0.0
  geolocator: ^14.0.2
  google_fonts: ^7.0.2
  intl: ^0.20.2
  latlong2: ^0.9.1
  location: ^8.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
